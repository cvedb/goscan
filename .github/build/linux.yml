# .github/build/linux.yml

# Build settings
builds:
  - id: linux
    goos: linux
    goarch: amd64
    main: ./cmd/myapp
    binary: myapp
    archive:
      format: tar.gz
    env:
      - CGO_ENABLED=0

# Release settings
release:
  github:
    owner: w3security
    name: goscan
